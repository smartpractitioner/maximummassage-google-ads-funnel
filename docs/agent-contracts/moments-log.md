# Moments log — factory design pointer index

> **Purpose:** A one-liner index of notable moments captured during MH work. Used at Phase 7 warm-up to identify which moments deserve expansion into full artifacts under `docs/agent-contracts/<station>-notes.md`. This file is a POINTER, not the archive.
>
> **What counts as notable:** non-obvious reasoning, refusing to edit when the instinct was to edit, framework generalizing to a novel case, a misfire that was self-corrected, a decision where the "obvious" answer turned out to be wrong. Routine work does NOT log. Bar: "would future-me want to look this up?"
>
> **Format per entry:** `YYYY-MM-DD — [station/task] — one-sentence why-notable.` One line only. No expansion. If you feel the urge to add a second line, resist — the pointer is enough; the full context is recoverable via git + the referenced files at Phase 7 expansion time.
>
> **When to expand:** at Phase 7 (factory buildout) warm-up. Cross-reference each entry with the git commits from that date + the current state of relevant files, then expand the most useful pointers into full artifacts.

---

## Entries (newest at the bottom)

- 2026-07-03 — [editor / Phase 2 Step E on prenatal] — worker flagged maternity (top-5, 480/mo) coverage gap, applied semantic-match reasoning ("recognize the face across the room"), recommended leave-as-is, voluntarily generalized to antenatal — a clean example of the flag-for-review pattern working as designed. Full artifact: [editor-station-notes.md](editor-station-notes.md).
- 2026-07-03 — [editor / Phase 2 overall pass on prenatal] — refused to edit an already-shipped-good page rather than editing to earn keep; declared "no changes recommended" as the phase deliverable — a clean example of the assessment-first stance surviving the pull to make changes.
- 2026-07-03 — [privacy design / Decision 9 firewall pivot] — misdiagnosed the Decision 9 gap as "code refactor to add session_id in one sheet"; user-driven correction pushed to two physically separate Google Sheets as the real technical firewall (access control at storage level, not policy-only). Pattern: when a firewall depends on policy for enforcement, look for a way to make the storage architecture enforce it.
- 2026-07-03 — [privacy design / consent framework] — Alberta playbook from Claude web session integrated verbatim as factory-general SOP for AB non-regulated professions; DIY approach adopted over CMP evaluation at this scale; multi-jurisdiction expansion parked for Phase 7 as regime complexity grows.
- 2026-07-03 — [privacy design / discovered stale legal docs] — discovered public/privacy-policy/ and public/terms/ pages had been in the repo since April 2026 but were written for the Landingi/Tally-era funnel and never updated when architecture migrated to native quiz + Apps Script + Sheets. Reconciliation approach adopted (update existing docs, not from-scratch draft) + architecture-agnostic language pattern to avoid another rewrite cycle at Cloudflare migration. Meta-lesson: retrospective scans of prior artifacts should be scoped broadly, not just to the specific workstream being worked on.
- 2026-07-07 — [privacy design / factory reference material integrated] — Victor supplied two Feb 2026 Claude-web-session docs (Canadian multi-jurisdiction compliance + CMP comparison), converted to MD and saved as factory SOPs. Elevates Enzuzo as concrete top CMP choice for factory scale (Waterloo-based, $5/domain agency plan). Flags a consent model tension between the Alberta playbook (informed implied) and the multi-jurisdiction SOP (express consent) — jurisdictionally scoped, requires legal counsel adjudication for MH.
- 2026-07-09 — [campaign structure / launch gate framing correction] — advisor kept drifting to "prenatal-only soft launch" framing; user-driven correction clarified that Google Ads campaigns are structured with multiple ad groups per campaign, each needing its own dedicated landing page for QS + message match. Launching one page doesn't launch a campaign — it leaves other ad groups without landing pages. Pattern for future clients: launch gate = "all landing pages that fill this client's campaign ad groups are ready," not "top-priority page is polished."
- 2026-07-09 — [image sourcing / selection principles + AI-generated hero] — Victor taught the image-selection framework (match theme/emotion/audience/content/roster, keep-it-real, client-assets-first, reuse-not-re-source) → encoded into sop-image-sourcing.md; shipped an AI-generated prenatal hero that beat all free stock because it could render a copy-specific detail (the side-lying bolster) that stock lacked. Signals for the factory's image-sourcing station: AI generation is a first-class source, and paid stock is back on the table when the image is load-bearing.
- 2026-07-09 — [factory design / designer station identified] — Victor surfaced a gap: the factory had a copywriter station + voice-capture discipline but no design equivalent. Added a brand-capture step (7.2a) + designer/brand-capture agent (7.6) on the "constant skeleton, variable skin" principle — derive per-client design tokens (color, type, shape language, logo, feel) from the client's existing brand into client-config.css, engine bones unchanged. Pattern: when a per-client dimension (voice, brand) needs consistent capture, it earns its own station, not per-page improvisation.
- 2026-07-14 — [CMP / build-vs-buy + scope discipline] — Victor proposed a DIY cookie-consent banner over paying for Enzuzo/Cookiebot; advisor agreed BUT first refused to design anything until verifying the one fact that could kill it (does Google require a certified CMP?). Verification found it is doubly inapplicable — publisher-scoped AND EEA/UK-scoped, while we are Canadian advertisers — so DIY is viable. Separately flagged that MH needs no banner at all under the Alberta playbook, keeping the whole build OFF the launch critical path. Pattern: when a build depends on an external-rule assumption, verify the rule before spending design effort; and check whether the client even needs the capability before scoping it.
- 2026-07-14 — [legal / client-sign-off liability model] — advisor first mis-recorded "no lawyer review" as an MH-specific risk acceptance; Victor corrected it to the actual agency model: we never take on the client's compliance liability, we guide them toward compliance and THE CLIENT signs off on their own legal pages before go-live. That makes it a REQUIRED factory step for every client, not a skipped/optional one — the opposite of what was written. Pattern: "we're not doing X" often isn't an absence of a control, it's a different allocation of who owns it — ask which before recording it as a gap.
- 2026-07-14 — [legal / stale-signoff flag, correctly overruled] — advisor flagged that MH's pre-project sign-off predated the Phase 6.5 doc rewrite and argued a fresh approval was needed; Victor overruled with the better argument — the client signed off on the PRINCIPLE (data held securely + compliantly), and the rewrite only made the storage description more accurate without weakening that commitment, so the sign-off carries. Victor's calibration note: flag material items like this for confirmation, but don't be overzealous and flag every little thing. Pattern for the factory's QA/reviewer stations: judge a change by whether it alters the substance the approver actually approved, not by whether the text diffed.
- 2026-07-15 — [booking UX / iframe limits diagnosed → custom-UI-on-Cal-API] — advisor twice asserted from memory that a Cal.com iframe setting could produce leadgenjay's fluid "tap-date → calendar collapses → slot column + back" mobile flow; Victor's on-device screenshots disproved it both times. Root cause finally identified: that reference calendar is a CUSTOM-rendered UI, not an iframe — which is why it's fluid. Resolution: build our own calendar on Cal.com's public API (GET /v2/slots, POST /v2/bookings) keeping Cal as the engine, so all downstream (webhook/attribution/Jane/conversion) survives. Pulled forward from Phase 8 (8.10+8.11) into canonical-template Phase 3.5 because the picker/quiz/booking is shared engine code — building it once at the source propagates to all pages + future clients. Patterns: (1) stop asserting external-tool behavior from memory after one miss — verify or defer; (2) when a third-party embed can't do what you need, the question is 'can we render it ourselves on their API', not 'which flag'; (3) shared-engine UX upgrades belong on the canonical template before rollout, not in the post-launch parking lot.
2026-07-15 — [picker/quiz Part A] — screenshots showed a Continue button but the brief said keep auto-advance; confirmed the fork with Victor before building rather than silently matching the visual.
2026-07-15 — [Part B booking] — built the custom calendar + Cal proxy, but committed only the dormant Pages Functions and HELD the front-end flip, to avoid replacing the working live iframe booking with a phone-fallback before keys+real-verification.
2026-07-17 — [page-speed 3.4] — Diagnosed PSI 8s LCP as Slow-4G lab-throttle (consistent) vs. 60–80 score spread as cold-edge/TTFB (variance); refused to buy Argo/Cache Reserve for a static Pages site and built a free cron cache-warmer for the real field-data path instead.
2026-07-18 — [page-speed 3.4] — Caught that a cold PSI run reported CLS 0 while the page truly had 0.107 (only the fast run tells the truth); nearly closed the step on that false signal, then fixed the root cause with computed size-adjust fallback metrics rather than per-element min-height whack-a-mole.
2026-07-18 — [page-speed 3.4] — Audited real font usage via computed styles instead of reading CSS and caught my own wrong claim ("we use one Bricolage weight" — actually 600/700 + two italics); found the cost was the opsz AXIS not the weight count, cutting the font payload 36%.
2026-07-18 — [page-speed 3.4] — Discovered the measurement tools had been reading a STALE hero for hours (my own 1-day image cache), meaning several PSI "findings" were about a file already fixed; added cache-busting to the SOP as a pre-measurement check.
2026-07-18 — [page-speed 3.4] — Predicted the _headers Cache-Control would not enable HTML edge-caching on Pages, tested it rather than assuming, and caught a wildcard-without-asterisk bug in the Cache Rule before deploy that would have silently no-opped the whole thing.
2026-07-20 — [quiz engine] — Kayla caught single-vs-multi on the LIVE page after a full QA loop had passed; adding multi-select surfaced a second correctness bug nobody asked about (catch-all options combining incoherently with specific ones), which I fixed rather than shipping the narrow ask.
2026-07-20 — [factory/legal] — Turned Victor's "harvest the client's attorney feedback" idea into a jurisdiction x modality ledger, and flagged the boundary risk he did not raise: a lesson must not cross a jurisdiction/modality line without re-checking regulated-profession status (AB massage unregulated, BC/ON regulated), plus confidentiality limits on reusing counsel work product.
